apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - configmap.yaml
  - postgres.yaml
  - backend.yaml
  - frontend.yaml
  - yolo-server.yaml
  - vlm-server.yaml
# Optional. Apply only after installing an ingress controller.
#  - ingress.yaml
