export function IssueListPage() {
  return (
    <div className="page-stack">
      <section className="page-title">
        <div>
          <h1>이슈 관리</h1>
        </div>
      </section>
      <section className="panel">
        <div className="panel-title">
          <h2>백엔드 API 준비 중</h2>
          <span>mock 제거됨</span>
        </div>
        <p className="muted">현재 프론트에서 실제로 연결된 API는 화재 감지 요청입니다. 이슈 목록은 조회 API가 생기면 연결할 수 있습니다.</p>
      </section>
    </div>
  );
}
