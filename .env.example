APP_ENV=dev

# Temporary local database. Replace when the final DB decision is made.
POSTGRES_DB=burinake
POSTGRES_USER=burinake
POSTGRES_PASSWORD=burinake_dev_password
POSTGRES_PORT=5432

BACKEND_PORT=8080
FRONTEND_PORT=5173
YOLO_SERVER_PORT=8001
VLM_SERVER_PORT=8002
YOLO_MODEL_DIR=/opt/burinake/models/yolo
VLM_MODEL_DIR=/opt/burinake/models/vlm

AZURE_STORAGE_CONNECTION_STRING=UseDevelopmentStorage=true
AZURE_BLOB_CONTAINER=fire-events
